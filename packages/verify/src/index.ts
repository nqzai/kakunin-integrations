/**
 * @kakunin/verify
 *
 * Thin re-export of the certificate-enforcement middleware that also ships as
 * `@kakunin/sdk/verify`. Published as a standalone package so the documented
 * `npm install @kakunin/verify` works on its own. All symbols come from
 * `@kakunin/sdk` — install either; you get the same middleware.
 */
export * from '@kakunin/sdk/verify';
